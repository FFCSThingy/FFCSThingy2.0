export interface TimetableBodyRowProps {
	rowNumber: number;
	isMobile?: boolean;
	isAfternoon?: boolean;
}

export default interface TimetableBodyProps {
	isMobile?: boolean;
	isAfternoon?: boolean;
}
