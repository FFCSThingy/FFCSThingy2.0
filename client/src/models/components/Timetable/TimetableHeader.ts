export interface TimetableHeaderRowProps {
	isMobile?: boolean;
	isAfternoon?: boolean;
	rowNumber: number;
};

export default interface TimetableHeaderProps {
	isMobile?: boolean;
	isAfternoon?: boolean;
};